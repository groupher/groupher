# Inspire Me

This app is built with TanStack Start and deployed to Cloudflare Workers. Keep
research data in `data/`, generate public JSON through `generate:data`, and
read it at runtime through the `ASSETS` Worker binding. Do not add Next.js or
Vinext configuration, runtime imports, or scripts here.
