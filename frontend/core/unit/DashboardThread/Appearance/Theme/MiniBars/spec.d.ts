export type TProps = {
  active: boolean
  primaryColor: string
  accentColor: string
  textTitle: string
  textDigest: string
}

export type TColorBarProps = {
  className?: string
  color: string
}

export type TTextBarProps = {
  titleColor: string
  digestColor: string
}
