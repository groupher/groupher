// The public tree has one shared bounded-depth legacy document. Keep the
// feature and page registries on the same source until the API is flattened.
import { docPublicTree } from '../../schemas/pages/doc'

export { docPublicTree }

export default {
  docPublicTree,
}
