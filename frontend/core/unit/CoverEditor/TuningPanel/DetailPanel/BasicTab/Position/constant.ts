export const KEYBOARD_DELTA = 0.03

// The tiny Position card reads visually tighter than the real cover, so its frame is reduced only in preview.
export const POSITION_PREVIEW_FRAME_SCALE = 0.88

export const GRID_VERTICAL_LINES = [20, 40, 60, 80]

export const GRID_HORIZONTAL_LINES = [25, 50, 75]
