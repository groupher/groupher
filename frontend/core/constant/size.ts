import type { TSizeH, TSizeL, TSizeM, TSizeS, TSizeT } from '~/spec'

const SIZE = {
  TINY: 'tiny' as TSizeT,
  SMALL: 'small' as TSizeS,
  MEDIUM: 'medium' as TSizeM,
  LARGE: 'large' as TSizeL,
  HUGE: 'huge' as TSizeH,
}

export default SIZE
