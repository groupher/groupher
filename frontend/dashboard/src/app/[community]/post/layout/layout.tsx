'use client'

import createPostLayout from '../_shared/createPostLayout'

export default createPostLayout({
  path: 'layout',
  title: 'dsb.post.layout',
  crumbTitle: 'dsb.crumb.post.layout',
  withDivider: true,
})
