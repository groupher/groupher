'use client'

import createChangelogLayout from '../_shared/createChangelogLayout'

export default createChangelogLayout({
  path: 'layout',
  title: 'dsb.changelog.layout',
  crumbTitle: 'dsb.crumb.changelog.layout',
  withDivider: true,
})
