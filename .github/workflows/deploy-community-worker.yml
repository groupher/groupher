name: Deploy Community Worker

on:
  pull_request:
    paths:
      - 'frontend/community/**'
      - 'frontend/core/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
  push:
    branches:
      - dev
    paths:
      - 'frontend/community/**'
      - 'frontend/core/**'
      - 'package.json'
      - 'pnpm-lock.yaml'

permissions:
  contents: read

concurrency:
  group: deploy-community-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Community Worker
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run type-check
        run: pnpm --filter @groupher/frontend-community run type-check

      - name: Run lint
        run: pnpm --filter @groupher/frontend-community run lint

      - name: Run community contract checks
        run: pnpm --filter @groupher/frontend-community run check:contract

      - name: Validate Worker deploy
        run: pnpm --filter @groupher/frontend-community run deploy:dry-run

  deploy:
    name: Deploy Community Worker
    needs: validate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Deploy Worker
        run: pnpm --filter @groupher/frontend-community run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
