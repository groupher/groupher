name: Frontend typecheck

on:
  pull_request:
    paths:
      - 'frontend/{dash,community,apply,landing,core}/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
      - 'frontend/**/tsconfig*.json'
      - 'frontend/**/package.json'
      - 'package.json'
      - 'yarn.lock'
  push:
    paths:
      - 'frontend/{dash,community,apply,landing,core}/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
      - 'frontend/**/tsconfig*.json'
      - 'frontend/**/package.json'
      - 'package.json'
      - 'yarn.lock'

permissions:
  contents: read

jobs:
  changes:
    name: Detect frontend typecheck changes
    runs-on: ubuntu-latest
    outputs:
      targets: ${{ steps.matrix.outputs.targets }}
      has_targets: ${{ steps.matrix.outputs.has_targets }}

    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            shared:
              - 'frontend/core/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
              - 'frontend/core/tsconfig*.json'
              - 'frontend/core/package.json'
              - 'package.json'
              - 'yarn.lock'
            dash:
              - 'frontend/dash/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
              - 'frontend/dash/tsconfig*.json'
              - 'frontend/dash/package.json'
            community:
              - 'frontend/community/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
              - 'frontend/community/tsconfig*.json'
              - 'frontend/community/package.json'
            apply:
              - 'frontend/apply/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
              - 'frontend/apply/tsconfig*.json'
              - 'frontend/apply/package.json'
            landing:
              - 'frontend/landing/**/*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}'
              - 'frontend/landing/tsconfig*.json'
              - 'frontend/landing/package.json'

      - name: Build matrix
        id: matrix
        env:
          SHARED_CHANGED: ${{ steps.filter.outputs.shared }}
          DASH_CHANGED: ${{ steps.filter.outputs.dash }}
          COMMUNITY_CHANGED: ${{ steps.filter.outputs.community }}
          APPLY_CHANGED: ${{ steps.filter.outputs.apply }}
          LANDING_CHANGED: ${{ steps.filter.outputs.landing }}
        run: |
          targets=()

          if [ "$SHARED_CHANGED" = "true" ]; then
            targets+=('{"target":"core","workspace":"@groupher/frontend-core"}')
          fi

          if [ "$SHARED_CHANGED" = "true" ] || [ "$DASH_CHANGED" = "true" ]; then
            targets+=('{"target":"dash","workspace":"@groupher/frontend-dash"}')
          fi

          if [ "$SHARED_CHANGED" = "true" ] || [ "$COMMUNITY_CHANGED" = "true" ]; then
            targets+=('{"target":"community","workspace":"@groupher/frontend-community"}')
          fi

          if [ "$SHARED_CHANGED" = "true" ] || [ "$APPLY_CHANGED" = "true" ]; then
            targets+=('{"target":"apply","workspace":"@groupher/frontend-apply"}')
          fi

          if [ "$SHARED_CHANGED" = "true" ] || [ "$LANDING_CHANGED" = "true" ]; then
            targets+=('{"target":"landing","workspace":"@groupher/frontend-landing"}')
          fi

          if [ ${#targets[@]} -eq 0 ]; then
            echo 'targets={"include":[]}' >> "$GITHUB_OUTPUT"
            echo 'has_targets=false' >> "$GITHUB_OUTPUT"
          else
            joined=$(printf '%s\n' "${targets[@]}" | paste -sd, -)
            echo "targets={\"include\":[${joined}]}" >> "$GITHUB_OUTPUT"
            echo 'has_targets=true' >> "$GITHUB_OUTPUT"
          fi

  typecheck:
    name: typecheck (${{ matrix.target }})
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.has_targets == 'true'
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.changes.outputs.targets) }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Enable Corepack
        run: corepack enable

      - name: Install deps
        run: yarn install --immutable

      - name: Run TypeScript type-check
        run: yarn workspace ${{ matrix.workspace }} type-check
