name: Deploy Assets Hub Worker

on:
  pull_request:
    paths:
      - 'backend/assets-hub/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
  push:
    branches:
      - dev
    paths:
      - 'backend/assets-hub/**'
      - 'package.json'
      - 'pnpm-lock.yaml'

permissions:
  contents: read

concurrency:
  group: deploy-assets-hub-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Assets Hub
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable Corepack
        run: corepack enable

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Run type-check
        run: pnpm --filter @groupher/assets-hub run type-check

      - name: Run lint
        run: pnpm --filter @groupher/assets-hub run lint

      - name: Build Node service
        run: pnpm --filter @groupher/assets-hub run build

      - name: Validate Worker deploy
        run: pnpm --filter @groupher/assets-hub run deploy:worker:dry-run

  deploy:
    name: Deploy Assets Hub Worker
    needs: validate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Enable Corepack
        run: corepack enable

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Deploy Worker
        run: pnpm --filter @groupher/assets-hub run deploy:worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
